# 安全说明

## 报告漏洞

如发现安全问题，请通过 GitHub Issues 私信维护者，**勿在公开 Issue 中粘贴密钥、数据库连接串或用户数据**。

## 密钥管理

- **切勿**将 `.env`、API Key、数据库密码提交到 Git。
- 仓库仅保留 `*.example` 模板文件；部署时在服务器或 CI 密钥库中配置真实值。
- 若密钥曾误提交，请立即轮换（Neon / DeepSeek / OpenAI 等），并清理 Git 历史。

## 默认试用密钥

`TRIAL_DEVELOPER_KEY` 用于高校/企业端试用门禁，**生产环境务必修改为强随机字符串**。
