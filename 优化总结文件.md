# 科技成果转化协同平台 — 优化总结

> 本文件记录 Phase 1–5 全部优化内容，含宏观概览与逐文件改动明细。

---

## 一、优化背景与目标

### 存在的问题

* 成果转化模块与原平台（/platform）**割裂**，缺乏统一导航
* 大量数据依赖 **前端 mock / 内存存储**
* 用户身份未接入业务逻辑（creatorId 硬编码为 `stu_001` / `school-user` 等）
* 多角色协同（高校 / 企业 / 学生）**不真实**
* 页面虽多，但缺乏统一使用路径

### 优化目标

> 将系统从"展示型 demo"升级为"可运行的半真实多角色协同平台"。

---

## 二、总体优化成果

| 升级维度 | 核心内容 |
|---|---|
| 架构升级 | 新增 transformation 模块，打通 Next.js → Python API → Service 层 |
| 协同升级 | 引入真实用户身份（Auth 接入），实现高校/企业/学生跨角色数据流 |
| 数据升级 | 新增 Prisma 模型，实现 DB + in-memory fallback 双模式 |
| 导航升级 | 三端左侧导航新增"成果转化"分组，发布成功跳转目标页面 |

---

## 三、Phase 1–5：新增与修改文件总览

### 1. 新增文件（7 个）

| 文件 | 说明 |
|---|---|
| `apps/web/lib/auth-handler.ts` | 服务端读取 `platform_token` httpOnly cookie，返回 `Authorization: Bearer` 头 |
| `apps/web/hooks/use-auth.ts` | 客户端 hook，调用 `/api/auth/me` 获取当前登录用户 |
| `apps/web/app/api/v1/demands/intentions/route.ts` | 补全缺失的 intentions POST 代理路由 |
| `apps/api/app/core/pool.py` | 同步 psycopg 连接池（懒初始化、`is_pool_available()`、`with_pool()`） |
| `apps/api/app/services/demand_service.py` | 需求 CRUD，DB 优先 + fallback |
| `apps/api/app/services/participation_service.py` | 学生参与管理 |
| `apps/api/app/schemas/demand.py` | 请求/响应数据模型 |

### 2. 修改文件（17 个）

#### API 代理层（Next.js Route Handlers）

| 文件 | 改动内容 |
|---|---|
| `app/api/v1/demands/route.ts` | 增加 `getAuthHeaders()` 转发 auth token |
| `app/api/v1/demands/[id]/route.ts` | 修复 Next.js 15 Promise params + 增加 auth 转发 |
| `app/api/v1/demands/participations/route.ts` | 增加 auth 转发 |
| `app/api/v1/demands/participations/student/[student_id]/route.ts` | 修复 Promise params + 增加 auth 转发 |
| `app/api/v1/achievements/route.ts` | 增加 POST 方法 + auth 转发 |
| `app/api/v1/achievements/[id]/route.ts` | 增加 auth 转发 |
| `app/api/v1/achievements/[id]/participate/route.ts` | 增加 auth 转发 |
| `app/api/v1/transformation/stats/route.ts` | 增加 auth 转发 |

#### 业务页面（替换硬编码用户 ID）

| 文件 | 改动内容 |
|---|---|
| `app/(platform)/student/transformation/page.tsx` | `stu_001` → `useAuth().user.id` |
| `app/(platform)/enterprise/transformation/post/page.tsx` | `enterprise-user` → `useAuth().user.id` |
| `app/(platform)/school/transformation/publish/page.tsx` | `school-user` → `useAuth().user.id` |
| `app/transformation/achievements/[id]/page.tsx` | mock 用户 → 真实 `useAuth()` 用户 |
| `app/transformation/demands/[id]/page.tsx` | mock 用户 → 真实 `useAuth()` 用户 |

#### 后端

| 文件 | 改动内容 |
|---|---|
| `apps/api/app/api/routes/achievement.py` | 增加 `POST /achievements/{id}/participate` 端点 |

### 3. 新增 API 路由清单

| 路由 | 方法 | 功能 |
|---|---|---|
| `/api/v1/demands` | GET / POST | 需求列表 / 发布需求 |
| `/api/v1/demands/{id}` | GET | 需求详情 |
| `/api/v1/demands/intentions` | POST | 对接意向申请 |
| `/api/v1/demands/participations` | POST | 学生报名参与 |
| `/api/v1/demands/participations/student/{id}` | GET | 学生参与记录 |
| `/api/v1/achievements` | GET / POST | 成果列表 / 发布成果 |
| `/api/v1/achievements/{id}` | GET | 成果详情 |
| `/api/v1/achievements/{id}/participate` | POST | 成果申请参与 |
| `/api/v1/transformation/stats` | GET | 转化统计 |

---

## 四、最小侵入式整合：Dashboard 入口修复

### 改动的文件

| 文件 | 改动内容 |
|---|---|
| `app/(platform)/school/dashboard/page.tsx` | 快捷入口新增"浏览成果广场" → `/transformation/achievements` |
| `app/(platform)/student/dashboard/page.tsx` | 常用入口新增"浏览需求广场" → `/transformation/demands` |

### 无需修改（已完整）

| 文件 | 原因 |
|---|---|
| `app/(platform)/enterprise/dashboard/page.tsx` | 已有"发布技术需求"和"浏览成果广场"两个入口 |
| `app/(platform)/school/transformation/publish/page.tsx` | 发布成功后已跳转 `/transformation/achievements` |
| `app/(platform)/enterprise/transformation/post/page.tsx` | 发布成功后已跳转 `/transformation/demands` |

---

## 五、最小侵入式导航整合：左侧导航栏

### 改动的文件

| 文件 | 改动内容 |
|---|---|
| `apps/web/components/layout/app-sidebar.tsx` | `navMap` 类型从 `NavItem[]` 升级为 `NavGroup[]`，支持分组标题；三角色各新增"成果转化"分组 |

### 各角色新增导航结构

**高校端（school）**

```
成果转化
  ├── 发布成果          → /school/transformation/publish
  └── 浏览成果广场      → /transformation/achievements
```

**企业端（enterprise）**

```
成果转化
  ├── 发布技术需求      → /enterprise/transformation/post
  └── 浏览成果广场      → /transformation/achievements
```

**学生端（student）**

```
成果转化
  ├── 转化工作台        → /student/transformation
  └── 浏览需求广场      → /transformation/demands
```

### 高亮机制

`pathname === item.href` 精确匹配，当前页自动高亮（青绿色背景 + 左侧竖条），无需额外代码。

---

## 六、Auth 链路（核心突破）

### 改造前

```
前端 mock user → 后端无身份
```

### 改造后

```
登录页 → platform_token (httpOnly cookie)
  → Next.js Route Handler 读取 cookie (via cookies())
  → Authorization: Bearer {token} 转发至 Python API
  → Python auth_service 验证 token
  → 写入真实 creatorId / studentId
```

### 实现效果

* creatorId 真实化
* studentId 真实化
* 多角色协同链路成立

---

## 七、数据流真实链路

| 链路 | 说明 |
|---|---|
| 高校 → 企业 | 发布成果 → 企业浏览成果广场可见 |
| 企业 → 学生 | 发布需求 → 学生浏览需求广场可见 |
| 学生 → 高校/企业 | 申请参与成果/需求 → 记录可查询 |

---

## 八、数据库层优化

### 数据存储模式

```
DB 可用 → 持久化写入 PostgreSQL
DB 不可用 → 系统 fallback 至内存 _demands_store 继续运行
```

### demand_service 核心函数

| 函数 | 功能 |
|---|---|
| `_db_list_demands()` | 参数化 SELECT，支持 domain/mode/budget 过滤 + ILIKE 搜索 |
| `_db_get_demand()` | SELECT by id |
| `_db_create_demand()` | INSERT，snake_case 列名对齐 Prisma schema |
| `_db_update_match_count()` | UPDATE match_count + updated_at |
| `_row_to_demand()` | DB 行（snake_case）→ Python camelCase dict |

---

## 九、当前系统状态

| 模块 | 状态 |
|---|---|
| UI | ✅ 完整 |
| API | ✅ 完整 |
| Auth | ✅ 已接入 |
| 多角色协同 | ✅ 已成立 |
| 导航整合 | ✅ 已完成 |
| 数据持久化 | ⚠️ 部分完成（demand_service 已落地，achievement_service 待跟进） |
| TAI / LSM | ❌ 未接入 |

---

## 十、仍待完成项

### P0（必须）

* Prisma DB 真正落地（`npx prisma db push --accept-data-loss`）
* achievement_service 参照 demand_service 模式落地数据库
* 认证隔离完善（防止跨用户数据写入）

### P1（重要）

* TAI 接入参与逻辑
* LSM 路径更新
* Dashboard 真实统计

### P2（增强）

* 政府端治理能力
* 智能推荐引擎
* 转化项目抽象

---

## 十一、总结

本轮优化实现了系统的关键跃迁：

> 从"前端演示系统" → "具备真实数据流与用户身份的协同平台"

核心突破：
* ✅ 多角色真实协同（Auth 链路打通）
* ✅ API 全链路打通（Next.js → Python → DB）
* ✅ 身份驱动数据写入（creatorId/studentId 真实化）
* ✅ 成果转化模块纳入平台导航体系
* ⚠️ 部分数据持久化（demand_service 已落地）

> **本轮优化不是功能扩展，而是将系统从"可展示"升级为"可运行"。**

---

## 十二、文件改动统计

| 类别 | 数量 |
|---|---|
| 新增文件 | 7 |
| 修改文件 | 17 |
| 涉及角色 | school / enterprise / student |
| 涉及层 | 前端 Route Handler、前端 Page、前端 Layout/Nav、后端 Service、后端 Route |
