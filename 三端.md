## 三端数据流通可优化点（Markdown 计划书）

### 背景：你现在已经打通的主链路
- **企业端**发布岗位 → 进入 `app_enterprise_jobs`
- **学生端**推荐从岗位池生成 → 写入 `app_job_recommendations`
- **学生端**投递到具体 `job_id` → 反查 `company_id` → 写入 `app_student_applications / app_application_packages`
- **企业端**按 `company_id` 拉取投递收件箱与投递包

下面是在此基础上，三端之间还能继续“打通/流通/闭环”的优化清单（不要求你立刻实现）。

---

## 1）把“投递”升级为跨三端的核心业务对象（单一事实来源）
### 目标
企业/学生/高校看到的是**同一份 Application 的不同视图**，并且能围绕它闭环协同。

### 建议改造
- **数据模型**
  - `app_applications`（建议新增，替代/吸收 `app_student_applications` 的业务语义）
    - `application_id (PK) / student_id / job_id / company_id`
    - `status`（统一状态机）
    - `created_at / updated_at`
  - `app_application_status_events`
    - `application_id / from_status / to_status / actor_role / actor_id / note / created_at`
- **三端联动**
  - 学生端：投递中心展示“状态时间线”
  - 企业端：收件箱可操作（推进状态、备注、分配负责人）
  - 高校端：在学生画像/干预任务里可看到“投递进展与卡点”

---

## 2）企业 → 学生：反馈回流闭环（把反馈绑定到投递包）
### 目标
企业的面试反馈/评分能回流到学生画像与推荐解释；学生也能看到“为什么没过/差在哪”。

### 建议改造
- **表结构**
  - `app_enterprise_feedback`
    - `feedback_id (PK)`
    - `application_id (FK)`（强绑定）
    - 冗余：`company_id/job_id/student_id`
    - `scores JSONB / conclusion TEXT / draft BOOL / created_by / created_at`
- **接口**
  - 企业端：`POST /enterprise/applications/{id}/feedback`
  - 学生端：`GET /students/{id}/applications/{application_id}/feedback`（仅本人可见）
- **回流策略**
  - 把 feedback 作为学生端“下一步训练建议”的输入（模拟器可用）

---

## 3）高校 → 企业：可控的人才推荐/背书流（带权限与脱敏）
### 目标
高校端不是只看数据，而是能把“优秀学生/项目经历/课程证据”以**可授权**方式同步给企业端。

### 建议改造
- **能力对象：Evidence（证据）**
  - `app_student_evidence`
    - `evidence_id / student_id / type(project/course/cert) / payload JSONB / created_at`
- **共享机制**
  - `app_evidence_shares`
    - `share_id / evidence_id / company_id / expires_at / scope`
- **三端联动**
  - 高校端：对企业“共享证据包”（可设置有效期/范围）
  - 企业端：候选人详情中看到“高校背书证据（脱敏/可下载权限）”
  - 学生端：看到“哪些证据被共享给了哪些企业”

---

## 4）推荐系统的“来源可追溯”（学生/企业/高校都能解释）
### 目标
推荐不是黑箱：为什么推这份岗位、依据来自模拟/简历/高校证据哪个部分，都能解释。

### 建议改造
- `app_job_recommendations` 增加：
  - `company_id`（你已补）
  - `signals JSONB`（来源信号：模拟分、简历fit、课程覆盖、证据命中）
  - `explain TEXT/JSONB`（可解释字段）
- 企业端：看到“推荐该岗位的学生画像理由”
- 高校端：看到“推荐与课程/能力缺口的对应关系”

---

## 5）跨端消息通知（不是实时聊天，但要有“事件推送”）
### 目标
企业更新状态、发起面试、提交反馈后，学生/高校能及时获知。

### 建议改造
- `app_notifications`
  - `notification_id / receiver_role / receiver_id / type / payload / read_at / created_at`
- 触发点
  - `application.status` 变化
  - `feedback` 创建/发布（draft→final）
  - 企业邀请面试（带时间地点/形式）

---

## 6）三端统一身份与权限收口（数据层强隔离）
### 目标
彻底解决“页面拦了但接口没拦”的风险；并支持企业多人协作/高校多人协作。

### 建议改造
- token 中明确：`role + user_id + (company_id/school_id)`
- 所有查询都必须带：
  - 企业端：强制 `company_id`
  - 高校端：强制 `school_id`
  - 学生端：强制 `student_id`
- 增加最小 RBAC：
  - enterprise：admin/recruiter/viewer
  - school：admin/counselor/viewer

---

## 7）“人才池”双来源合并（模拟人才池 + 投递人才池）
### 目标
企业端人才池既能看模拟高潜，也能看真实投递，且可区分来源、互相跳转。

### 建议改造
- 企业端 talent pool 增加筛选维度：
  - `source=simulation | application | shared_by_school`
- 列表项统一指向：
  - 有投递：跳到 `application` 详情
  - 无投递：跳到候选人画像（模拟）

---

## 推荐实施顺序（最小闭环优先）
- **P0（闭环必须）**：Application 状态机 + 事件表；企业反馈绑定 application；学生端展示投递进度
- **P1（跨端协同）**：高校证据共享；通知系统；人才池双来源
- **P2（可解释增强）**：推荐 signals/explain；岗位版本化；审计/导出治理

---

如果你要我把它落成你仓库里的文件，我会新增一个（不改你现有 plan）：`docs/tri-party-dataflow-optimizations.md`，并按 P0/P1/P2 拆成可直接开发的任务列表。你想更偏 **“拿奖演示闭环”** 还是更偏 **“可上线合规/权限”** 来写优先级？