## 交接文档（给新 Agent 的上下文总结）

### 1) 项目概况
- **仓库**：`https://github.com/Harryfox-tech/adaptlink.git`
- **结构**：Monorepo
  - `apps/web`：Next.js 15 + TS + Tailwind（平台三端 UI）
  - `apps/api`：FastAPI + psycopg + Postgres（Neon）后端
- **核心目标**：打通学生端投递 ↔ 企业端岗位来源 ↔ 企业账号可见性（company 维度），并上线（Vercel + Railway）

---

### 2) 已完成：数据打通（按计划落地）
#### 后端（FastAPI）
- **企业/公司结构**
  - 新增 `app_enterprise_companies`
  - `app_auth_enterprise` 增加 `company_id/role`
- **岗位池落库**
  - `app_enterprise_jobs` 落库
  - 企业端：
    - `GET /api/v1/enterprise/jobs` 按 company_id 返回本公司岗位
    - `POST /api/v1/enterprise/jobs` 创建岗位（归属 company）
- **投递写入结构化外键**
  - `app_student_applications` 增加 `job_id/company_id`
  - `app_application_packages` 增加 `job_id/company_id`
  - 学生端投递 `POST /students/{id}/applications/submit`：
    - 强校验 `job_id` 必须存在于企业岗位池
    - 反查出 `company_id` 并写入投递记录
- **企业端收件箱 API**
  - `GET /enterprise/applications`（按 company_id 过滤）
  - `GET /enterprise/applications/{application_id}/package`
- **推荐来源切换**
  - 无历史推荐时，从 `app_enterprise_jobs(status=open)` 生成推荐并回写 `app_job_recommendations`
- **鉴权**
  - `/enterprise/*` 增加 token 校验 + company_id 强制过滤
  - 注意：会话 token 仍为内存（进程重启要重新登录），但足够 demo

#### 前端（Next.js）
- 新增/调整：
  - 企业端新增页面：`/enterprise/applications` + `/enterprise/applications/[id]`
  - Sidebar 增加“投递收件箱”入口
  - `StudentApplication` 类型补 `jobId/companyId`
  - 企业端页面从 cookie 取 token 传给 API（SSR 页面）
  - 为 client 组件调用 enterprise POST 增加 Next API 代理路由（`apps/web/app/api/enterprise/...`）
- 企业端注册/登录流程升级：
  - 新增：`/register/company`（先注册公司获取 company_id）
  - 注册/登录企业端时必须输入 `companyId`（后端也强制校验）
  - Next 的 `/api/auth/*` 路由支持 companyId 透传

---

### 3) 已完成：部署准备 & 上线进展
#### Git
- 已完成首次提交并持续推送修复
- 删除了无效 `vercel.json`（曾导致 Vercel 报 `rootDirectory` 不允许）
- Next 升级到 `15.0.7`（Railway 曾因漏洞阻断构建）

#### Railway（后端）
- **后端域名**：`https://adaptlink-production.up.railway.app`
- 解决了多次 Railway 部署问题：
  - Railway 误部署了 `@platform/web`（前端 workspace）→ 应删除
  - Dockerfile `COPY` 路径修复（从 repo root 复制 `apps/api/...`）
  - Start Command 变量不展开问题：最终统一 **固定端口 8080**
- 当前后端应可用：
  - `GET https://adaptlink-production.up.railway.app/health` 返回 ok（用户已拿到域名）

#### Vercel（前端）
- 已在 Vercel 创建项目时遇到过：
  - 环境变量配置缺少 `https://` 导致 fetch parse URL 错误
- 需要正确配置：
  - `NEXT_PUBLIC_API_BASE_URL=https://adaptlink-production.up.railway.app/api/v1`
- Vercel 上 `/api/chat` 报错：
  - `OPENAI_API_KEY 未配置`（这是 Vercel 环境变量问题，不是后端挂）

---

### 4) 当前阻塞点 / 待确认
1) **Railway Variables** 是否已设置：
   - 必填：`DATABASE_URL`（Neon）
   - 若没配，会出现线上登录提示“连接 127.0.0.1:5432 被拒绝”（已出现过）
2) **Vercel Variables** 是否设置正确：
   - `NEXT_PUBLIC_API_BASE_URL` 必须带 `https://` 且带 `/api/v1`
3) **AI Key 放哪**：
   - Vercel 的 `/api/chat` 要用真实模型 → 在 Vercel 配 `OPENAI_API_KEY/BASE_URL/MODEL`
   - FastAPI 侧也可用真实 LLM → Railway 配 `AI_PROVIDER=openai` + key
4) Railway 计费与休眠：
   - 用户发现 Replica 不能设为 0；建议删掉失败的 `@platform/web` 服务、或删服务以停费

---

### 5) 下一步建议（按优先级）
#### P0：让线上“能登录能跑通链路”
- 在 Railway 后端服务 Variables 设置：
  - `DATABASE_URL=<neon连接串>`
  - `AI_PROVIDER=mock`（先确保业务链路通）
- 在 Vercel 设置：
  - `NEXT_PUBLIC_API_BASE_URL=https://adaptlink-production.up.railway.app/api/v1`
- 验收路径：
  - 后端：`/health`
  - 前端：注册公司 → 企业注册/登录 → 企业 dashboard / 收件箱
  - 学生端：推荐/投递 → 企业端收件箱可见

#### P1：AI 功能恢复（按你需求选）
- 若要 Vercel `/api/chat` 可用：
  - 在 Vercel 加 `OPENAI_API_KEY/OPENAI_BASE_URL/OPENAI_MODEL`（DeepSeek 用 `https://api.deepseek.com/v1`）
- 若要后端真实 LLM：
  - Railway 加 `AI_PROVIDER=openai` + `OPENAI_*`

#### P2：成本控制
- 删除 Railway 里 `@platform/web` 失败服务
- 若需要不计费：删除后端服务（记下变量），需要演示再重建

---

## 给新 Agent 的“启动指令”（你复制过去即可）

```markdown
你在 Cursor 里接手 adaptlink 项目部署与联调。

仓库：https://github.com/Harryfox-tech/adaptlink.git
后端（Railway）：https://adaptlink-production.up.railway.app
目标：让 Vercel 前端能正确调用 Railway 后端 + Neon 数据库，并让登录/投递/企业收件箱链路在线上跑通；然后按需恢复 AI（Vercel /api/chat 或 FastAPI LLM）。

请先做：
1) 检查 Railway 后端 Variables 是否已配置 DATABASE_URL（Neon）。若未配置，给出配置清单并确认后端不再尝试连接 127.0.0.1:5432。
2) 检查 Vercel 环境变量 NEXT_PUBLIC_API_BASE_URL 是否为 https://adaptlink-production.up.railway.app/api/v1 并触发 redeploy。
3) 线上验证：/health、登录、学生投递、企业收件箱可见。
4) 若 /api/chat 报 OPENAI_API_KEY missing，则指导用户在 Vercel 配 OPENAI_*，或把缺 key 的报错降级为 mock 提示（可选）。

注意：
- Railway 上曾误建过 @platform/web 服务，可建议删除。
- Railway startCommand/port 展开曾有问题，当前固定端口 8080；确保 public networking 指向 8080，healthcheck /health。
```

---

## 你如何“重新开一个 Agent”继续工作
- 在 Cursor 里新开对话/新 Agent 后，把上面两段内容贴过去：
  - **交接文档**
  - **启动指令**
- 然后把你当前的两项关键信息也贴进去：
  - 你的 **Neon `DATABASE_URL` 是否已填入 Railway（是/否）**
  - 你的 **Vercel 项目域名**（`xxx.vercel.app`）

如果你愿意，我也可以把这份交接 Markdown 直接写进仓库（例如 `HANDOFF.md`），方便你随时复制给别的同学/新 Agent。